import { useCallback, useEffect, useRef, useState } from 'react'
export function useRecording() { const recorder = useRef<MediaRecorder | null>(null); const stream = useRef<MediaStream | null>(null); const chunks = useRef<Blob[]>([]); const startedAt = useRef(0); const [isRecording,setIsRecording]=useState(false); const [isPaused,setIsPaused]=useState(false); const [duration,setDuration]=useState(0); const [audioBlob,setAudioBlob]=useState<Blob|null>(null); const [error,setError]=useState<string|null>(null); useEffect(()=>{ if(!isRecording||isPaused)return; const timer=window.setInterval(()=>setDuration(Math.floor((Date.now()-startedAt.current)/1000)),250); return ()=>window.clearInterval(timer)},[isRecording,isPaused]); const startRecording=useCallback(async()=>{ setError(null); if(!navigator.mediaDevices?.getUserMedia||!window.MediaRecorder){setError('Recording is not supported in this browser.');return} try { stream.current=await navigator.mediaDevices.getUserMedia({audio:true}); chunks.current=[]; const next=new MediaRecorder(stream.current); next.ondataavailable=e=>e.data.size&&chunks.current.push(e.data); next.onerror=()=>setError('The recording stopped unexpectedly.'); next.onstop=()=>{setAudioBlob(new Blob(chunks.current,{type:next.mimeType||'audio/webm'})); stream.current?.getTracks().forEach(t=>t.stop()); stream.current=null}; recorder.current=next; startedAt.current=Date.now(); setDuration(0); next.start(); setIsRecording(true); setIsPaused(false) } catch { setError('Microphone permission was denied or unavailable.') }},[]); const pauseRecording=useCallback(()=>{if(recorder.current?.state==='recording'){recorder.current.pause();setIsPaused(true)}},[]); const resumeRecording=useCallback(()=>{if(recorder.current?.state==='paused'){recorder.current.resume();setIsPaused(false)}},[]); const stopRecording=useCallback(()=>{if(recorder.current&&recorder.current.state!=='inactive'){recorder.current.stop();setIsRecording(false);setIsPaused(false)}},[]); const resetRecording=useCallback(()=>{if(recorder.current?.state!=='inactive')recorder.current?.stop();stream.current?.getTracks().forEach(t=>t.stop());recorder.current=null;stream.current=null;chunks.current=[];setAudioBlob(null);setDuration(0);setError(null);setIsRecording(false);setIsPaused(false)},[]); return {isRecording,isPaused,duration,audioBlob,error,startRecording,pauseRecording,resumeRecording,stopRecording,resetRecording} }
