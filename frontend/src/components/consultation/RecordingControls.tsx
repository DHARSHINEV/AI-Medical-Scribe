import { Mic, Pause, Play, RotateCcw, Square } from 'lucide-react'
import { Button } from '../layout/AppShell'
export function RecordingControls({recording}:{recording:{isRecording:boolean;isPaused:boolean;audioBlob:Blob|null;error:string|null;startRecording:()=>void;pauseRecording:()=>void;resumeRecording:()=>void;stopRecording:()=>void;resetRecording:()=>void}}){return <div className="recording-controls"><Button primary disabled={recording.isRecording} onClick={recording.startRecording}><Mic size={15}/> Start Recording</Button><Button disabled={!recording.isRecording||recording.isPaused} onClick={recording.pauseRecording}><Pause size={15}/> Pause</Button><Button disabled={!recording.isRecording||!recording.isPaused} onClick={recording.resumeRecording}><Play size={15}/> Resume</Button><Button disabled={!recording.isRecording} onClick={recording.stopRecording}><Square size={15}/> Stop</Button><Button disabled={!recording.isRecording&&!recording.audioBlob} onClick={recording.resetRecording}><RotateCcw size={15}/> Reset</Button>{recording.audioBlob&&<span className="recording-ready">Captured audio ready</span>}{recording.error&&<span className="form-error">{recording.error}</span>}</div>}
