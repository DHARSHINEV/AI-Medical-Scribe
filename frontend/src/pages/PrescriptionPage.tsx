import { useState } from 'react'
import { Download, Printer, Save, ShieldCheck } from 'lucide-react'
import { useParams } from 'react-router-dom'
import { Button, Notice, PageTitle, Panel } from '../components/layout/AppShell'
import { useConsultation } from '../hooks/useConsultation'
import type { MedicationItem } from '../types/prescription'
export default function PrescriptionPage(){const {id=''}=useParams();const c=useConsultation(id);const [medications,setMedications]=useState<MedicationItem[]>([]);const [advice,setAdvice]=useState('');const [followUp,setFollowUp]=useState('');const [saved,setSaved]=useState(false);const addMedication=()=>setMedications(current=>[...current,{id:crypto.randomUUID(),name:'',strength:'',dose:'',route:'Oral',frequency:'',duration:'',quantity:'',instructions:''}]);const update=(medicationId:string,key:keyof MedicationItem,value:string)=>setMedications(current=>current.map(m=>m.id===medicationId?{...m,[key]:value}:m));return <><Notice/><PageTitle eyebrow={`PRESCRIPTION · ${c.consultation.id}`} title="Prepare prescription"><span className="status-pill"><i/> Draft · clinician review required</span></PageTitle><div className="prescription-layout"><Panel title="Prescription" label="No medications are pre-populated"><div className="document-header"><b>MediScribe</b><span>Clinical prescription draft · {new Date().toLocaleDateString()}</span></div><div className="patient-banner"><div><small>Patient</small><strong>{c.currentPatient.name}</strong><span>{c.currentPatient.age} · {c.currentPatient.sex} · {c.currentPatient.mrn}</span></div><div className="allergy-banner"><b>Allergies</b><span>{c.currentPatient.allergies.join(', ')}</span></div></div><label className="field-label">Assessment<input defaultValue={c.note.Assessment}/></label><div className="table-wrap"><table><thead><tr>{['Medication','Strength','Dose','Route','Frequency','Duration','Quantity','Instructions'].map(h=><th key={h}>{h}</th>)}</tr></thead><tbody>{medications.map(m=><tr key={m.id}>{(['name','strength','dose','route','frequency','duration','quantity','instructions'] as const).map(key=><td key={key}><input aria-label={key} value={m[key]} onChange={e=>update(m.id,key,e.target.value)}/></td>)}</tr>)}</tbody></table></div><Button onClick={addMedication}>Add medication</Button><div className="form-grid"><label className="field-label">Non-pharmacological advice<textarea value={advice} onChange={e=>setAdvice(e.target.value)}/></label><label className="field-label">Follow-up instructions<textarea value={followUp} onChange={e=>setFollowUp(e.target.value)}/></label></div><div className="signature-box"><ShieldCheck size={20}/><span><b>Clinician signature required</b><small>Draft has not been sent or signed.</small></span></div><div className="review-actions"><Button primary onClick={()=>setSaved(true)}><Save size={15}/> {saved?'Saved':'Save draft'}</Button><Button onClick={()=>window.print()}><Printer size={15}/> Print</Button><Button onClick={()=>{const blob=new Blob([JSON.stringify({consultationId:c.consultation.id,patient:c.currentPatient,medications,advice,followUp},null,2)],{type:'application/json'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=`${c.consultation.id}-prescription.json`;a.click();URL.revokeObjectURL(url)}}><Download size={15}/> Export JSON</Button></div></Panel></div></>}
