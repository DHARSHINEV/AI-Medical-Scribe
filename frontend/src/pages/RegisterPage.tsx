import { FormEvent, useMemo, useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { UserPlus } from 'lucide-react'
import { useAuth } from '../context/AuthContext'
export default function RegisterPage(){const {register,error,isLoading}=useAuth();const navigate=useNavigate();const [name,setName]=useState('');const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [role,setRole]=useState('Clinician');const [validation,setValidation]=useState('');const strength=useMemo(()=>password.length>=12?'Strong':password.length>=8?'Moderate':'Needs 8+ characters',[password]);const submit=async(e:FormEvent)=>{e.preventDefault();if(!name||!email||password.length<8||password!==confirm){setValidation(password!==confirm?'Passwords do not match.':'Complete all fields; password must be at least 8 characters.');return}if(await register({name,email,password,role}))navigate('/',{replace:true})};return <main className="auth-page"><div className="auth-card"><div className="brand auth-brand"><span className="brandmark">M+</span><span><b>MediScribe</b><small>Clinical documentation workspace</small></span></div><span className="eyebrow">CLINICIAN REGISTRATION</span><h1>Create your account</h1><form onSubmit={submit} className="auth-form"><label>Full name<input value={name} onChange={e=>setName(e.target.value)} autoComplete="name"/></label><label>Professional role<select value={role} onChange={e=>setRole(e.target.value)}><option>Clinician</option><option>Physician</option><option>Nurse practitioner</option><option>Medical assistant</option></select></label><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email"/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="new-password"/><small className="password-strength">{strength}</small></label><label>Confirm password<input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} autoComplete="new-password"/></label>{(validation||error)&&<p className="form-error">{validation||error}</p>}<button className="btn primary wide" disabled={isLoading} type="submit"><UserPlus size={16}/>{isLoading?'Creating account…':'Create account'}</button></form><p className="auth-footer">Already registered? <Link to="/login">Back to sign in</Link></p></div></main>}
