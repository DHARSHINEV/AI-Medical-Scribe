import { FormEvent, useState } from 'react'
import { Link, useLocation, useNavigate } from 'react-router-dom'
import { Eye, EyeOff, LogIn } from 'lucide-react'
import { useAuth } from '../context/AuthContext'
export default function LoginPage(){ const { login, enterDemoMode, error, isLoading }=useAuth(); const navigate=useNavigate(); const location=useLocation(); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [show,setShow]=useState(false); const [validation,setValidation]=useState(''); const next=new URLSearchParams(location.search).get('next')||'/'; const submit=async(e:FormEvent)=>{e.preventDefault(); if(!email||!password){setValidation('Enter your email and password.');return} const ok=await login(email,password); if(ok) navigate(next,{replace:true})}; return <main className="auth-page"><div className="auth-card"><div className="brand auth-brand"><span className="brandmark">M+</span><span><b>MediScribe</b><small>Clinical documentation workspace</small></span></div><span className="eyebrow">SECURE CLINICIAN ACCESS</span><h1>Sign in to your workspace</h1><p className="muted">Use your organization credentials to access patient and consultation data.</p><form onSubmit={submit} className="auth-form"><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email"/></label><label>Password<span className="password-field"><input type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password"/><button type="button" aria-label={show?'Hide password':'Show password'} onClick={()=>setShow(!show)}>{show?<EyeOff size={16}/>:<Eye size={16}/>}</button></span></label>{(validation||error)&&<p className="form-error">{validation||error}</p>}<button className="btn primary wide" disabled={isLoading} type="submit"><LogIn size={16}/>{isLoading?'Signing in…':'Sign in'}</button></form><div className="auth-divider">or</div><button className="btn secondary wide" onClick={()=>{enterDemoMode();navigate(next,{replace:true})}}>Enter labeled Demo Mode</button><p className="auth-footer">New to MediScribe? <Link to="/register">Create an account</Link></p></div></main> }
